<Window x:Class="EQRisk.Desktop.Views.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource DialogWindow}"
        Title="EQRisk">
    <Grid Margin="28,24,28,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="Heading" Style="{StaticResource H2}" FontSize="18"
                   MouseLeftButtonDown="OnDrag" />
        <TextBlock x:Name="Message" Grid.Row="1" Style="{StaticResource Body}" Foreground="{StaticResource Ink2Brush}"
                   LineHeight="21" Margin="0,2,0,22" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Style="{StaticResource SecondaryButton}" IsCancel="True" Margin="0,0,10,0"
                    Click="OnCancel" />
            <Button x:Name="ConfirmButton" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="OnConfirm" />
        </StackPanel>
    </Grid>
</Window>
