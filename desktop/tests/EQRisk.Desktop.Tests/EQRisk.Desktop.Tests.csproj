<Project Sdk="Microsoft.NET.Sdk">

  <!-- The layer boundaries (by assembly reference), the app's service graph, and the pieces of the
       Windows app that can run without a window: the tray icon renderer and the value converters. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <UseWPF>true</UseWPF>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\EQRisk.Desktop\EQRisk.Desktop.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="xunit.v3" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
